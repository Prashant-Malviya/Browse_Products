# Base URL of the running backend (see ../README.md in the backend repo).
# The documented backend only exposes GET /api/products today — the
# stats/seed endpoints this frontend also calls are not yet part of that
# contract (see the API page in the app for details).
VITE_API_BASE_URL=http://localhost:8080
